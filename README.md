# Home Automation Project

## Configuration

See `.env` for recommended environment variables. You can control logging using the `LOG_LEVEL` environment variable (DEBUG/INFO/WARNING/ERROR). Default: `INFO`.

Use `config/README.md` for additional configuration tips if present.
