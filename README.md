# Home Automation Project

## Configuration

See `.env.example` for recommended environment variables and `config/README.md` for details on how to configure and run services (edge vs server, Docker vs host).
